/**
 * VoiceSelectorModal -- re-exported from VoiceSelector.tsx where it is
 * co-located with VoiceSelector (the popover variant) to share the API
 * import, helper types, and avoid a duplicated ~22 KB module.
 *
 * All existing imports of this path continue to work unchanged.
 */
export type { VoiceSelectorModalProps } from "@/components/VoiceSelector";
export { VoiceSelectorModal } from "@/components/VoiceSelector";
